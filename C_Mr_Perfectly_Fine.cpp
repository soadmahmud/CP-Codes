#include <bits/stdc++.h>
using namespace std;

int main()
{
    int test;

    cin >> test;

    while(test--)
    {
        int n;

        cin >> n;

        int array[n];

        string s[n];

        for(int i = 0; i < n; i++)
            cin >> array[i] >> s[i];

        int first = 100000000, second = 100000000, both = 100000000;

         for(int i = 0; i < n; i++)
        {
            if(s[i] == '10')
            {
                if(array[i] <= first)
                    first = 
            }
        }
    }
}